function(mwtl_add_agent_eval name)
    add_library(mwtl_agent_eval_${name} OBJECT "${name}.cpp")
    target_compile_features(mwtl_agent_eval_${name} PRIVATE cxx_std_20)
    target_compile_options(mwtl_agent_eval_${name} PRIVATE /W4 /permissive- /EHsc /utf-8)
    target_link_libraries(mwtl_agent_eval_${name} PRIVATE mwtl::mwtl)
    set_property(GLOBAL APPEND PROPERTY MWTL_AGENT_EVAL_TARGETS mwtl_agent_eval_${name})
endfunction()

foreach(name IN ITEMS window form layout commands background dialog placement dpi appearance native_message notepad_setting tray_command property_page split_pane virtual_list_column document_workspace docking_workspace)
    mwtl_add_agent_eval(${name})
endforeach()

mwtl_add_agent_eval(native_host)

function(mwtl_add_optional_agent_eval name component)
    if(NOT TARGET mwtl::${component})
        return()
    endif()
    add_library(mwtl_agent_eval_${name} STATIC "${name}.cpp")
    target_compile_features(mwtl_agent_eval_${name} PRIVATE cxx_std_20)
    target_compile_options(mwtl_agent_eval_${name} PRIVATE /W4 /permissive- /EHsc /utf-8 /WX)
    target_link_libraries(mwtl_agent_eval_${name} PRIVATE mwtl::${component})
    if(component STREQUAL "scintilla")
        mwtl_deploy_scintilla(mwtl_agent_eval_${name})
    endif()
    set_property(GLOBAL APPEND PROPERTY MWTL_AGENT_EVAL_TARGETS mwtl_agent_eval_${name})
endfunction()

mwtl_add_optional_agent_eval(webview2_navigation webview2)
mwtl_add_optional_agent_eval(scintilla_notification scintilla)
mwtl_add_optional_agent_eval(direct2d_scene d2d)
mwtl_add_optional_agent_eval(direct3d_recovery d3d)
mwtl_add_optional_agent_eval(printed_page printing)
mwtl_add_optional_agent_eval(print_preview printing)
mwtl_add_optional_agent_eval(ole_custom_format ole)
mwtl_add_optional_agent_eval(ole_drop_target ole)
mwtl_add_optional_agent_eval(jump_list_task shell)
mwtl_add_optional_agent_eval(taskbar_progress shell)
mwtl_add_optional_agent_eval(file_association shell)
mwtl_add_optional_agent_eval(typed_setting shell)
mwtl_add_optional_agent_eval(ribbon_workspace ribbon)
mwtl_add_optional_agent_eval(mdi_workspace mdi)
mwtl_add_optional_agent_eval(graphics_export graphics)
mwtl_add_optional_agent_eval(taskbar_help shell)

get_property(mwtl_agent_eval_targets GLOBAL PROPERTY MWTL_AGENT_EVAL_TARGETS)
add_custom_target(mwtl_agent_eval_fixtures DEPENDS ${mwtl_agent_eval_targets})

set(MWTL_AGENT_EVAL_CANDIDATE "" CACHE FILEPATH "Optional coding-agent candidate source")
set(MWTL_AGENT_EVAL_COMPONENT "mwtl" CACHE STRING "Component for the coding-agent candidate")
if(MWTL_AGENT_EVAL_CANDIDATE)
    if(NOT EXISTS "${MWTL_AGENT_EVAL_CANDIDATE}")
        message(FATAL_ERROR "MWTL_AGENT_EVAL_CANDIDATE does not exist")
    endif()
    add_library(mwtl_agent_eval_candidate OBJECT "${MWTL_AGENT_EVAL_CANDIDATE}")
    target_compile_features(mwtl_agent_eval_candidate PRIVATE cxx_std_20)
    target_compile_options(mwtl_agent_eval_candidate PRIVATE /W4 /permissive- /EHsc /utf-8 /WX)
    if(NOT TARGET mwtl::${MWTL_AGENT_EVAL_COMPONENT})
        message(FATAL_ERROR "Unknown Agent eval component: ${MWTL_AGENT_EVAL_COMPONENT}")
    endif()
    target_link_libraries(mwtl_agent_eval_candidate PRIVATE mwtl::${MWTL_AGENT_EVAL_COMPONENT})
endif()
