function(mwfl_add_example target)
    add_executable(${target} WIN32
        ${ARGN}
        "${CMAKE_CURRENT_FUNCTION_LIST_DIR}/example.manifest")
    target_compile_features(${target} PRIVATE cxx_std_20)
    target_compile_options(${target} PRIVATE /W4 /permissive- /EHsc /utf-8)
    target_link_libraries(${target} PRIVATE mwfl::ui)
    set_property(
        SOURCE "${CMAKE_CURRENT_FUNCTION_LIST_DIR}/example.manifest"
        PROPERTY VS_TOOL_OVERRIDE "Manifest")
endfunction()

function(mwfl_add_cli_example target)
    add_executable(${target} ${ARGN})
    target_compile_features(${target} PRIVATE cxx_std_20)
    target_compile_options(${target} PRIVATE /W4 /permissive- /EHsc /utf-8)
endfunction()

function(mwfl_add_core_example target)
    mwfl_add_cli_example(${target} ${ARGN})
    target_link_libraries(${target} PRIVATE mwfl::core)
endfunction()
function(mwfl_add_service_example target)
    mwfl_add_cli_example(${target} ${ARGN})
    target_link_libraries(${target} PRIVATE mwfl::service)
endfunction()
function(mwfl_add_process_example target)
    mwfl_add_cli_example(${target} ${ARGN})
    target_link_libraries(${target} PRIVATE mwfl::process)
endfunction()
function(mwfl_add_ipc_example target)
    mwfl_add_cli_example(${target} ${ARGN})
    target_link_libraries(${target} PRIVATE mwfl::ipc)
endfunction()
function(mwfl_add_diagnostics_example target)
    mwfl_add_cli_example(${target} ${ARGN})
    target_link_libraries(${target} PRIVATE mwfl::diagnostics)
endfunction()
function(mwfl_add_security_example target)
    mwfl_add_cli_example(${target} ${ARGN})
    target_link_libraries(${target} PRIVATE mwfl::security)
endfunction()
function(mwfl_add_deployment_example target)
    mwfl_add_cli_example(${target} ${ARGN})
    target_link_libraries(${target} PRIVATE mwfl::deployment)
endfunction()
function(mwfl_add_foundation_stack_example target)
    mwfl_add_cli_example(${target} ${ARGN})
    target_link_libraries(${target} PRIVATE mwfl::process mwfl::ipc mwfl::diagnostics)
endfunction()
function(mwfl_add_foundation_overview_example target)
    mwfl_add_cli_example(${target} ${ARGN})
    target_link_libraries(${target} PRIVATE mwfl::core mwfl::service mwfl::process mwfl::ipc mwfl::diagnostics mwfl::security mwfl::deployment)
endfunction()

add_subdirectory(hello)
add_subdirectory(application)
add_subdirectory(window)
add_subdirectory(native_message)
add_subdirectory(keyboard)
add_subdirectory(mouse)
add_subdirectory(resize)
add_subdirectory(timer)
add_subdirectory(paint)
add_subdirectory(minmax)
add_subdirectory(close_policy)
add_subdirectory(window_state)
add_subdirectory(dpi)
add_subdirectory(window_options)
add_subdirectory(wait_aware)
add_subdirectory(wakeup)
add_subdirectory(com_sta)
add_subdirectory(cli_basic)
add_subdirectory(cli_worker)
add_subdirectory(core_foundation)
add_subdirectory(service_host)
add_subdirectory(process_runner)
add_subdirectory(ipc_framed)
add_subdirectory(diagnostics_pipeline)
add_subdirectory(security_dpapi)
add_subdirectory(deployment_restart)
add_subdirectory(foundation_stack)
add_subdirectory(foundation_overview)
add_subdirectory(controls)
add_subdirectory(common_controls)
add_subdirectory(self_drawn_host)
add_subdirectory(system_lifecycle)
add_subdirectory(hot_corners)
add_subdirectory(form_binding)
add_subdirectory(commands)
add_subdirectory(desktop_integration)
add_subdirectory(document_state)
add_subdirectory(notepad)
add_subdirectory(document_workspace)
add_subdirectory(mdi_workspace)
add_subdirectory(ribbon_workspace)
add_subdirectory(graphics_interop)
add_subdirectory(docking_workspace)
add_subdirectory(appearance)
add_subdirectory(layout_gallery)
add_subdirectory(property_sheet)
add_subdirectory(explorer)
add_subdirectory(drawing)
add_subdirectory(image_viewer)
add_subdirectory(printing)
add_subdirectory(ole_drag_drop)
add_subdirectory(shell_integration)
if(MWFL_BUILD_SCINTILLA)
    add_subdirectory(code_editor)
endif()
if(MWFL_BUILD_WEBVIEW2)
    add_subdirectory(browser)
endif()
