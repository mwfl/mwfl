# Foundation core

Demonstrates the dependency-light `mwfl::core` component introduced in 0.1.1:
strict UTF conversion, explicit native error values, owned kernel handles, and
a wait that composes with `std::stop_token`.
