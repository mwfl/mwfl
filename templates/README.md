# Application templates

- `basic-app`: smallest recommended native window skeleton.
- `form-app`: explicit model, validation, focus recovery, and responsive form.

Copy a directory into a new project. The default consumption path pins mwtl to
the `v0.1.0` release; set `MWTL_SOURCE_DIR` to compile against a local checkout.
